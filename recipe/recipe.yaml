context:
  version: "2.1.3"

package:
  name: evalidate
  version: ${{ version }}

source:
  - url: https://pypi.org/packages/source/e/evalidate/evalidate-${{ version }}.tar.gz
    sha256: da49e12b813c1e76b104ec4ac8a566bdbd9ec46305febd90a6de2c889198926e

build:
  number: 0
  script: ${{ PYTHON }} -m pip install .
  noarch: python

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling >=1.27.0
    - wheel
    - pip
  run:
    - python >=${{ python_min }}

tests:
  - python:
      imports:
        - evalidate
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  summary: Validation and secure evaluation of untrusted python expressions
  license: MIT
  license_file: LICENSE
  homepage: https://github.com/yaroslaff/evalidate
  repository: https://github.com/yaroslaff/evalidate

extra:
  recipe-maintainers:
    - jaimergp
